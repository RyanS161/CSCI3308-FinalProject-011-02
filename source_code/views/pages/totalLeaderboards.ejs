<%- include ('../partials/head') %>
<%- include ('../partials/menu') %>

<main>
  <div class="container">
    <h1>Total Leaderboards</h1>
    <%- include('../partials/message') %>
    <% if (locals.totalLeaderboards) { %>
      <table class="table table-striped">
        <thead>
          <tr>
            <th scope="col">Rank</th>
            <th scope="col">Username</th>
            <th scope="col">Total Score</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i = 0; i < totalLeaderboards.length; i++) { %>
          <tr>
            <th scope="row"><%= i + 1 %></th>
            <td><%= totalLeaderboards[i].username %></td>
            <td><%= totalLeaderboards[i].sum_score %></td>
          </tr>
          <% } %>
        </tbody>
      <% } %>
  </div>
</main>

<%- include ('../partials/footer') %>